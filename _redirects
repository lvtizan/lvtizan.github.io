# 内部文件不对外暴露
#
# 本仓库 = 部署源本身，CF Pages 直接部署根目录、没有构建步骤过滤，
# 所以 push 上去的任何文件都会公开在 https://yoyant.com/<路径>。
# 2026-08-02 发现 CLAUDE.md / TODO.md / docs/plans/*.md / scripts/deploy-ecs.sh
# 全部返回 200 —— 内部规范、SEO 关键词策略、部署脚本（含服务器 IP 与 root 用户名）
# 都在公网可读。
#
# ⚠️ 状态码只能用 301/302/303/307/308 或 200。**不要写 404**——CF Pages 不支持，
#    写了会导致整份规则被判无效、一条都不生效（2026-08-02 实际踩过，
#    表现是所有内部文件照常返回 200，很容易误判成"规则没部署"）。
#
# ⚠️ 新增任何"非站点内容"的目录时，记得同步在这里加一条。
# ⚠️ 真正的密钥一律不进仓库（走环境变量），本文件只是防止内部资料被随手翻到，不是保险箱。

/docs/*           /  302
/scripts/*        /  302
/.githooks/*      /  302
/.superpowers/*   /  302
/CLAUDE.md        /  302
/README.md        /  302
/TODO.md          /  302
/.gitignore       /  302

# 已下架的样板站（2026-08-03）：BEDROCK / Aperture Capital / NORTHBEAM
# 三个面向英美本土服务业的单页 demo，与"给中国外贸厂建站"的定位不搭，
# 且首页零引用 + noindex，已是死资产。文件已删（git 历史可恢复），
# 但保留 301 —— 早年可能把链接发给过客户，落 404 不如落到案例区。
/work/demo-construction/*  /#cases  301
/work/demo-finance/*       /#cases  301
/work/demo-roofing/*       /#cases  301
/work/demo-construction    /#cases  301
/work/demo-finance         /#cases  301
/work/demo-roofing         /#cases  301
